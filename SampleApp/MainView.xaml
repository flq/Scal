<Window x:Class="SampleApp.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
        xmlns:dx="clr-namespace:DynamicXaml.MarkupSystem;assembly=DynamicXaml" Title="MainWindow" Height="250" Width="525">
    <Grid dx:QuickGrid.With="3x*|2x*">
        <TextBlock x:Name="Hello"></TextBlock>
        <ContentControl x:Name="ADate" Grid.Row="1" />
        <ContentControl x:Name="Number" Grid.Row="2" />
        <Button x:Name="SendMessage" Grid.Row="0" Grid.Column="1">Send Message</Button>
    </Grid>
</Window>
